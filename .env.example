# Copy to .env and fill in. Use a dedicated DB role in production (not the superuser).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/teak_production?schema=public"
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
PORT=4000
